version: "1.0"
workspace_root: "./test_sandbox"

agent_permissions:
  admin_agent:
    - write_file
    - read_file
  guest_agent:
    - read_file

allowed_tools:
  read_file:
    arguments:
      file_path: string
  write_file:
    arguments:
      file_path: string
      content: string